#ifndef X_AUDIO_INDEVICE
#define X_AUDIO_INDEVICE

class XInputDevice {
   public:
    // 构造XInputDevice
    XInputDevice();
    // 析构XInputDevice
    virtual ~XInputDevice();
};

#endif  // X_AUDIO_INDEVICE
